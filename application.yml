services:
  - type: web
    name: Discord-MusicBot-4
    runtime: python
    buildCommand: pip install -r requirements.txt && apt-get update && apt-get install -y ffmpeg libopus-dev
    startCommand: python main.py
